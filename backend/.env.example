# Railway injects DATABASE_URL when you add a PostgreSQL plugin
DATABASE_URL="postgresql://postgres:geKPLLhySjTmxnQZFJnKUopnwDgkrxjl@mainline.proxy.rlwy.net:59930/railway"

# Generate a long random string for production
JWT_SECRET="kdgGkQhLbZaw7b6AwNtmYmiinMwl6kh9S34eug4KnuTGQE3Iad2fz9ls7LpH9LR/"

# Server (Railway sets PORT in production, e.g. 8080 — do not rely on a fixed port locally)
PORT=3000
NODE_ENV=development

# Production API hostname (for docs only; clients use https://nyihaapp-production-4ca7.up.railway.app)
# Optional: comma-separated *browser* origins allowed by CORS (Flutter web / admin sites), not the API URL itself
CORS_ORIGINS="http://localhost:8080,http://localhost:3000,http://localhost:52734"

# Password reset links in emails use this public base (no trailing slash), e.g. https://your-api.railway.app
PASSWORD_RESET_PUBLIC_URL="https://your-api-host.example"

# Mail (required in production for real reset emails). Alternative: set SMTP_URL=smtps://user:pass@smtp.example:465
MAIL_FROM="Nyiha Society <noreply@yourdomain.com>"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
# Or: SMTP_URL="smtps://user:pass@smtp.example.com:465"
